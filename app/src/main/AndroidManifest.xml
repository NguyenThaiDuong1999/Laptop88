<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.laptop88">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activity.ThongTinActivity"
            android:label="@string/title_activity_thong_tin"></activity>
        <activity android:name=".activity.Staff" />
        <activity android:name=".activity.SanPhamDaMua" />
        <activity android:name=".activity.SanPhamYeuThich" />
        <activity android:name=".activity.SanPhamDaDat" />
        <activity android:name=".activity.SanPhamDaXem" />
        <activity android:name=".activity.Me" />
        <activity android:name=".activity.BaoHanhHauMai" />
        <activity android:name=".activity.Hang" />
        <activity android:name=".activity.ThongTinCaNhan" />
        <activity android:name=".activity.Search" />
        <activity android:name=".activity.DoanhNhan" />
        <activity android:name=".activity.GamingDoHoa" />
        <activity android:name=".activity.SinhVienVanPhong" />
        <activity android:name=".activity.TatCaSanPham" />
        <activity android:name=".activity.Admin" />
        <activity android:name=".activity.DangNhap" />
        <activity android:name=".activity.ThongTinKhachHang" />
        <activity android:name=".activity.GioHang" />
        <activity android:name=".activity.ChiTietSanPham" />
        <activity android:name=".activity.PhuKien" />
        <activity android:name=".activity.LaptopCu" />
        <activity android:name=".activity.LaptopMoi" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>